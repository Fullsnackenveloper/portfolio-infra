services:
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - web

  psi-monitor:
    image: ghcr.io/fullsnackenveloper/psi-monitor:latest
    container_name: psi-monitor
    restart: unless-stopped
    env_file:
      - ./psi-monitor.env
    volumes:
      - psi_data:/app/data
    networks:
      - web

lead-inbox:
    image: ghcr.io/fullsnackenveloper/lead-inbox:latest
    container_name: lead-inbox
    restart: unless-stopped
    networks:
      - web

networks:
  web:

volumes:
  caddy_data:
  caddy_config:
  psi_data: